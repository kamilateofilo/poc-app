// Home.tsx
import React from 'react';
import { View, Text } from 'react-native';

const Notificacoes: React.FC = () => {
  
  
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Página Notificações</Text>
    </View>
  );
};

export default Notificacoes;
